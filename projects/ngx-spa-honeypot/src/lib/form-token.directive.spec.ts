import { FormTokenDirective } from './form-token.directive';

describe('FormTokenManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new FormTokenDirective();
    expect(directive).toBeTruthy();
  });
});
